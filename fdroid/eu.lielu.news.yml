# Brouillon de recette F-Droid pour SwiperNews (eu.lielu.news).
#
# Ce fichier n'est PAS lu par fdroidserver depuis ce dépôt : F-Droid construit
# ses métadonnées dans son propre dépôt, gitlab.com/fdroid/fdroiddata, sous
# metadata/eu.lielu.news.yml. Ce brouillon sert de base à copier/coller dans
# une merge request là-bas (voir fdroid/README.md pour la procédure).
#
# Rempli à partir de .github/workflows/android.yml, qui fait le même travail
# pour produire l'APK GitHub — la recette F-Droid doit rester équivalente,
# sans dépendre de GitHub Actions ni d'un token.

Categories:
  - News
  - Reading
License: MIT
AuthorName: lielu.eu
WebSite: https://news.lielu.eu
SourceCode: https://github.com/notsogeek87/swipernews
IssueTracker: https://github.com/notsogeek87/swipernews/issues
Changelog: https://github.com/notsogeek87/swipernews/releases

# Ni Name ni Description ici : F-Droid reprend le titre et les descriptions
# depuis fastlane/metadata/ du dépôt source, qui sont la version faisant foi.
# Les déclarer ici les figerait, et les deux textes divergeraient.
#
# AutoName, lui, n'est pas un choix : « fdroid checkupdates » le remplit tout
# seul depuis l'android:label de l'app, et leur CI échoue si le fichier n'est
# pas déjà dans l'état qu'elle produirait.
AutoName: SwiperNews

RepoType: git
Repo: https://github.com/notsogeek87/swipernews.git

# Build reproductible (« RB »). F-Droid recompile depuis les sources, télécharge
# l'APK publié ici et vérifie que les deux sont identiques (signature exclue) ;
# si oui, c'est NOTRE binaire, signé de NOTRE clé, qui est distribué. Un même
# paquet s'installe donc par-dessus l'autre, qu'il vienne de F-Droid ou de
# GitHub — impossible quand F-Droid resigne avec sa propre clé.
#
# %v est remplacé par le versionName. L'URL est celle que produit
# .github/workflows/release.yml sur un tag vX.Y.Z : ce workflow compile
# exactement comme F-Droid (Gradle nu, sans -PversionCode/-PversionName), sinon
# les deux APK divergeraient sur la version et la vérification échouerait.
#
# Chez eux, cette ligne est repliée — « Binaries: », UNE ESPACE, saut de ligne,
# puis l'URL indentée de deux espaces : « fdroid rewritemeta » coupe à 80
# colonnes et leur CI refuse tout écart. Ici, le repli n'a pas lieu d'être, ce
# fichier n'étant pas celui qu'ils lisent (voir fdroid/README.md).
Binaries: https://github.com/notsogeek87/swipernews/releases/download/v%v/swipernews-%v.apk

# L'ORDRE DES CHAMPS compte : `Binaries` vient AVANT le bloc Builds,
# `AllowedAPKSigningKeys` APRÈS (voir plus bas). C'est celui de
# « fdroid rewritemeta », et leur CI refuse tout fichier qui n'est pas déjà
# dans l'état qu'elle produirait — les deux champs vont pourtant de pair, et
# rien ne laisse deviner qu'ils se rangent de part et d'autre. Vérifié sur
# metadata/org.briarproject.briar.android.yml chez eux.
Builds:
  - versionName: "1.3.0"
    versionCode: 10300
    # Hash complet du commit à compiler, et non le tag `v1.2.0` : un tag peut
    # être déplacé, un hash non — F-Droid l'exige pour que la recette désigne
    # un arbre de sources immuable. (Ici, le commit du tag v1.3.0.)
    #
    # Le couple ci-dessus doit correspondre à ce que produit un Gradle nu à ce
    # commit : android/app/build.gradle porte les mêmes valeurs, écrites en
    # clair, faute de -PversionCode/-PversionName ici. Les deux se bougent
    # ensemble.
    #
    # Le versionCode suivait ici l'ancien barème (majeur × 10000 + mineur × 100
    # + correctif). Élargi depuis en majeur × 10⁸ + mineur × 10⁶ + correctif ×
    # 10⁴, pour laisser à android.yml un palier où ranger ses builds de main —
    # voir fdroid/README.md. Cette entrée-ci n'est PAS à retoucher : elle décrit
    # ce que produit un Gradle nu au commit ci-dessous, qui n'a pas bougé.
    commit: 6294b6f4b6904f38a014ebc4b44c3e4e43c028da
    subdir: android
    # Le nodejs de Debian stable est trop vieux : la CLI Capacitor 8 exige
    # Node 22 et refuse Node 20 (d'où le setup-node@22 d'android.yml). D'où
    # forky (testing), suffisamment récent — plutôt qu'un « curl | bash » vers
    # NodeSource, qui ferait dépendre le build d'un script tiers exécuté à
    # l'aveugle. C'est la forme demandée en revue par licaon-kter.
    sudo:
      - echo "deb https://deb.debian.org/debian forky main" > /etc/apt/sources.list.d/forky.list
      - apt-get update
      - apt-get install -y -t forky npm
    gradle:
      - yes
    # `output:` est INDISPENSABLE ici, malgré la demande de duckniii de le
    # supprimer — l'avoir retiré a fait échouer leur build sur « Failed to find
    # any output apks », Gradle ayant pourtant réussi. Sans ce champ,
    # fdroidserver ne regarde que trois chemins, tous relatifs à `subdir`
    # (build.py, omethod == 'gradle') : `build/outputs/apk/release`,
    # `build/outputs/apk` et `build/apk`. Or notre APK naît un cran plus bas,
    # dans le module `app`. Renseigner `output:` bascule sur omethod == 'raw',
    # qui prend le chemin tel quel. C'est pourquoi le bloc de suggestion de
    # licaon-kter le conservait : ce n'était pas un oubli.
    output: app/build/outputs/apk/release/app-release-unsigned.apk
    #
    # cap:sync régénère www/ (non versionné) puis le recopie dans le projet
    # natif : sans lui, l'APK embarque un assets/public vide. « prebuild »
    # s'exécute dans subdir, donc dans android/ — pas à la racine, où vit le
    # package.json. Ne pas ajouter de « cd .. » pour autant : npm remonte tout
    # seul jusqu'au package.json le plus proche.
    prebuild:
      - npm ci
      - npm run cap:sync
    # F-Droid refuse tout binaire pré-compilé trouvé dans l'arbre des sources,
    # et npm ci en dépose une poignée dans node_modules (gabarits .tar.gz de la
    # CLI Capacitor, JAR de @trapezedev, binaires de sharp et de tsc). Aucun
    # n'entre dans l'APK, et aucun n'est utilisé par Gradle — @capacitor/android,
    # le seul module node_modules dont dépend le build (voir
    # android/capacitor.settings.gradle), n'en contient pas.
    #
    # « scandelete » ne supprime pas le dossier : le scanner n'efface que les
    # fichiers qu'il incrimine, et il tourne APRÈS prebuild, donc après cap sync.
    # « scanignore » est testé avant lui (scanner.py), l'entrée ci-dessous reste
    # donc « utilisée » et ne déclenche pas d'erreur « Unused scanignore path ».
    #
    # Tout ceci disparaîtrait en sortant @capacitor/assets des devDependencies
    # (il n'est utilisé que par npm run android:assets, pour régénérer les
    # icônes) : c'est lui qui apporte sharp, tsc et les JAR de trapezedev. Pas
    # touché tant que la MR est en revue : ces deux entrées sont exactement
    # celles qui ont passé leur scanner, et le champ est strict dans les deux
    # sens (un chemin disparu comme un chemin devenu inutile est une erreur).
    scanignore:
      - node_modules/@capacitor/assets/node_modules/@capacitor/cli/assets/capacitor-cordova-android-plugins.tar.gz
    scandelete:
      - node_modules

  # Version courante. L'entrée 1.3.0 ci-dessus est CONSERVÉE bien qu'elle ne
  # soit plus la dernière : c'est celle qui a déjà passé leur scanner et leur
  # build, elle vaut donc preuve que la recette fonctionne. Une entrée qui
  # échouerait ne ferait pas retomber l'autre.
  #
  # Rien d'autre ne change d'une entrée à l'autre : seuls versionName,
  # versionCode et commit bougent. Le versionCode suit ici le barème élargi
  # (majeur × 10⁸ + mineur × 10⁶ + correctif × 10⁴), contrairement à l'entrée
  # 1.3.0 qui date de l'ancien — c'est normal, chacune décrit ce que produit un
  # Gradle nu à SON commit.
  - versionName: "1.4.1"
    versionCode: 104010000
    commit: 5f54a5c6001c93bcb197f5ba2ed1f6d65c94c499
    subdir: android
    sudo:
      - echo "deb https://deb.debian.org/debian forky main" > /etc/apt/sources.list.d/forky.list
      - apt-get update
      - apt-get install -y -t forky npm
    gradle:
      - yes
    output: app/build/outputs/apk/release/app-release-unsigned.apk
    prebuild:
      - npm ci
      - npm run cap:sync
    scanignore:
      - node_modules/@capacitor/assets/node_modules/@capacitor/cli/assets/capacitor-cordova-android-plugins.tar.gz
    scandelete:
      - node_modules

# Empreinte SHA-256 du certificat de signature (celui des secrets ANDROID_* du
# dépôt). F-Droid refuse tout APK signé d'une autre clé, ce qui protège contre
# une release GitHub contrefaite. Relevée sur l'APK publié :
#   apksigner verify --print-certs swipernews-X.Y.Z.apk
#
# Sa place est ICI, après Builds, et non à côté de `Binaries` — voir le
# commentaire sur l'ordre des champs plus haut.
AllowedAPKSigningKeys: cc849a79fc39de5f8b4af5fcda1dbdb4230ad554aef85b484e5c7fd49caa6238

# versionName et versionCode sont écrits en clair dans android/app/build.gradle,
# donc F-Droid sait les lire : une nouvelle entrée Builds s'ajoute toute seule à
# chaque tag de version, sans merge request. C'est ce qui oblige à garder ces
# valeurs littérales là-bas (voir le commentaire du fichier).
#
# « Version » tout court, sans le « v%v » d'antan : leur schéma ne l'accepte
# plus, fdroidserver déduit seul le motif du tag depuis UpdateCheckMode.
AutoUpdateMode: Version
# Restreint aux tags de version : le dépôt porte aussi des tags android-v*
# posés par la CI à chaque build, qui ne sont pas des versions publiées.
UpdateCheckMode: Tags ^v[0-9]+\.[0-9]+\.[0-9]+$
CurrentVersion: "1.4.1"
CurrentVersionCode: 104010000

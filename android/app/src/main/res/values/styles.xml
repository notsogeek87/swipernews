<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- Navigateur intégré : thème SOMBRE (et non DayNight) volontairement — le
         fil est sombre en permanence, le lecteur doit l'être aussi. C'est aussi
         ce qui autorise l'assombrissement automatique des pages web
         (setAlgorithmicDarkeningAllowed ne s'applique qu'à un thème sombre). -->
    <style name="AppTheme.Reader" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/reader_bg</item>
        <item name="android:statusBarColor">@color/reader_bar</item>
        <item name="android:navigationBarColor">@color/reader_bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>